pipeline {
	agent any

	stages {
	 	stage('Do something') {
	 		steps {
		 		sh "date"
		 		sh "ls -la"
		 		sh "echo 'first pipeline doin great'"
	 		}
	 	}
	}
	
	post {
    	success {
    		echo "This is success!"
    	}
    
    	failure {
    		echo "Failure on dev branch."
    	}
  	}	
}
